PORT=5000
# Encode special characters in password: @ → %40, # → %23, : → %3A
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/credflow?retryWrites=true&w=majority
JWT_SECRET=your_jwt_secret
